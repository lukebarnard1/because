{
  "name": "because",
  "version": "0.0.0",
  "description": "Logging and diagnostics for reason-based debugging.",
  "main": "because.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "@lb:ldbco.de",
  "license": "ISC"
}
